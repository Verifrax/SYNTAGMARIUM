# Freeze object history

This directory stores historical freeze objects only.

Nothing here may be interpreted as the active freeze identity while a current freeze object exists under `freeze/objects/current/`.
